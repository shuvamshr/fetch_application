import 'package:flutter/material.dart';
import 'package:fetch_application/constants/colors.dart';

const Icon leftArrowIcon = Icon(
  Icons.chevron_left,
  color: gray_900,
  size: 24,
);

const Icon rightArrowIcon = Icon(
  Icons.chevron_right,
  color: gray_900,
  size: 24,
);

const Icon historyIcon = Icon(
  Icons.history,
  color: gray_900,
  size: 24,
);

const Icon checkmarkIcon = Icon(
  Icons.check_circle,
  color: green_500,
  size: 24,
);

const Icon addIcon = Icon(
  Icons.add,
  color: white,
  size: 32,
);
